# Core
requests>=2.31
PyYAML>=6.0
python-dotenv>=1.0

# Optional: LLM creative summary (only needed if you enable analysis.llm)
anthropic>=0.40

# Dashboards:
#  - streamlit/pandas: interactive app (streamlit run dashboard.py)
#  - matplotlib: PNG dashboard image export (cli `dashboard` command)
# The static HTML dashboard itself needs no extra deps (Chart.js via CDN).
streamlit>=1.30
pandas>=2.0
matplotlib>=3.7

# Dev / test
pytest>=8.0
